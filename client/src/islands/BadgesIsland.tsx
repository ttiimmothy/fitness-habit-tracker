import {QueryProvider} from "../providers/QueryProvider";
import { Badges } from './react/Badges';

export const BadgesIsland = () => {
  return (
  <QueryProvider>
    <Badges />
  </QueryProvider>
)}
