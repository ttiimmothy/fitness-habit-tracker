import { GooglePasswordCheck } from './react/GooglePasswordCheck';

export const GooglePasswordCheckIsland = ({ children }: { children: React.ReactNode }) => {
  return <GooglePasswordCheck>{children}</GooglePasswordCheck>;
};
